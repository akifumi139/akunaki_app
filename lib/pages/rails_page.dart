import 'package:flutter/material.dart';

class RailsPage extends StatelessWidget {
  const RailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Rails Page'));
  }
}
